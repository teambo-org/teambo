(function(t){
    "use strict";
/*
    var el = document.getElementById('buckets');
    t.team.bucket.all().then(function(buckets) {
        if(!buckets.length) {
            return;
        }
        var html = '';
        buckets.forEach(function(bucket) {
            html += t.view.render('index/_bucket-li', bucket);
        });
        el.innerHTML = html + el.innerHTML;
    });
*/
})(Teambo);
