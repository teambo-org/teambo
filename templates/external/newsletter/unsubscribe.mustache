<div class="external c">
    <div id="logo" class="logo" alt="Teambo"></div>
    <div class="main half-bg">
    <form id="unsubscribe" name="unsubscribe" method="POST" class="std ext" data-email="{{email}}" data-chk="{{chk}}">
        <div id="status"></div>
        <div class="field first">
            <p>Click the button below<br>to unsubscribe from the Teambo newsletter</p>
            <p class="email">{{email}}</p>
        </div>
        <div class="field submit">
            <input id="submit" type="submit" class="submit" value="Unsubscribe" />
        </div>
        <div class="error"></div>
    </form>
    </div>
    <div class="subtext" id="onboarding">
        <p>
            <a href="#/login" class="bot-nav"><i class="icon-angle-left"></i>Back to Login<i class="icon-blank"></i></a>
        </p>
    </div>
</div>
