<div class="item-view" require-model="item">
{{#model.item.current}}
    <nav class="breadcrumbs">
        <a href="#{{team.url}}">Team</a>
        {{#model.plan.current}}
        <a href="#{{url}}" data-keybind="q escape `">Plan</a>
        {{/model.plan.current}}
        {{^model.plan.current}}
        <a href="#{{#folder}}{{url}}{{/folder}}" data-keybind="q escape `">Folder</a>
        {{/model.plan.current}}
        <span>Item</span>
    </nav>
    <nav>
        <a href="#{{url}}/edit" data-keybind="e">Edit</a>
    </nav>
    <h1>{{#status}}<i class="icon-{{icon}}"></i>{{/status}} {{opts.name}}</h1>
    <div class="up4c3 {{^opts.description}}c-empty{{/opts.description}}">
    {{#opts.description}}
        <section class="description color-bg" role="main">
            {{#nl2br}}{{#linkify}}{{opts.description}}{{/linkify}}{{/nl2br}}
        </section>
    {{/opts.description}}
    </div>
    <div class="up4c up4c3b">
        {{>team/item/_sidenav}}
    </div>
    {{>team/comment/_section}}
    {{>team/history/_section}}
{{/model.item.current}}
</div>
