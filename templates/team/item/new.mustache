<div class="item-new" require-model="bucket">
{{#model.bucket.current}}
    <nav class="breadcrumbs">
        <a href="#{{team.url}}">Team</a>
        <a href="#{{url}}" data-keybind="q escape `">Bucket</a>
        <span>New Item</span>
    </nav>
    <h1><i class="icon-folder-open"></i> {{opts.name}}</h1>
    <form id="item-new" name="item_new" method="POST" class="std">
        <input name="bucket_id" type="hidden" value="{{id}}"/>
        <div class="field">
            <label for="item_name">Item Name</label>
            <input id="item_name"type="text" class="text large required min-len-2" name="name" autocomplete="off" placeholder="Item Name"/>
        </div>
        <div class="field">
            <label for="item_description">Description</label>
            <textarea id="item_description" class="text large full" name="description"></textarea>
        </div>
        <div class="field">
            <label for="item_status">Status</label>
            <select id="item_status" name="status" class="text large"></select>
        </div>
        <div class="field">
            <label for="item_plan">Plan</label>
            <select id="item_plan" name="plan_id" class="text large">
                <option value="">(none)</option>
            </select>
        </div>
        <div class="field submit">
            <input type="submit" class="submit" value="Create Item" />
            <a class="cancel" href="#{{url}}">cancel</a>
        </div>
        <div class="error"></div>
    </form>
{{/model.bucket.current}}
</div>
