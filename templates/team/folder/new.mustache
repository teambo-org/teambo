<div class="folder-new">
    <nav class="breadcrumbs">
        <a href="#{{team.url}}" class="active" data-keybind="q escape `">Team</a>
        <span class="active">New Folder</span>
    </nav>
    <h1><i class="icon-folder"></i> New Folder</h1>
    <form id="folder-new" name="folder_new" method="POST" class="std">
        <div class="field">
            <label for="folder_name">Name</label>
            <input id="folder_name" type="text" class="text required min-len-2" name="name" autocomplete="off" placeholder="Folder Name" />
        </div>
        <div class="field">
            <label for="folder_description">Description</label>
            <textarea id="folder_description" class="text" name="description"></textarea>
        </div>
        <div class="field submit">
            <input type="submit" class="submit" value="Create Folder" />
            <a class="cancel" href="#{{team.url}}">cancel</a>
        </div>
        <div class="error"></div>
    </form>
</div>
